
# Web framework and extensions
Flask==3.1.3
Flask-Cors==6.0.0
Flask-SocketIO==5.6.1

# WSGI server (production) — replaces the Werkzeug dev server
gunicorn==23.0.0
gevent==24.11.1
gevent-websocket==0.10.1

# Authentication
bcrypt==4.2.1

# Data processing and analysis
numpy==1.26.4  # Keep 1.26.x for compatibility with TensorFlow Lite

# Audio processing

# Machine Learning
tflite-runtime==2.14.0  # Keep stable version for model compatibility
onnxruntime>=1.16.0  # ONNX Runtime for BirdNET V3.0 model

# Requests
requests>=2.32.4

# Notifications
apprise>=1.8.0
paho-mqtt>=2.0.0

# Timezone utilities
timezonefinder>=6.2.0

# Visualization (spectrogram rendering)
Pillow>=10.4.0

# File system monitoring
watchdog==6.0.0

# Audio conversion (system dependency, not installable via pip)
sox==1.5.0
